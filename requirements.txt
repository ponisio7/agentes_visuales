# Core
PyQt6>=6.5.0
openai>=1.0.0
requests>=2.31.0

# Análisis de datos
numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0

# Formato de documentos (Fase B)
python-docx>=1.1.0
openpyxl>=3.1.0
reportlab>=4.0.0
markdown>=3.5.0
# Verificación de PDF (imágenes incrustadas en el contrato de aceptación, H6)
pypdf>=4.0.0

# Utilidades
plyer>=2.1.0
joblib>=1.3.0
scikit-learn>=1.4.0

# Aprendizaje semántico: matcher A/B (H2) y retrieval de casos similares (H8).
# sentence-transformers usa torch como backend. Son dependencias REALES:
# learning/embedding_matcher.py importa sentence_transformers y el motor de
# embeddings no funciona sin ellas (la app degrada, pero el retrieval no).
# El modelo (paraphrase-multilingual-MiniLM-L12-v2, ~470 MB) se descarga la
# primera vez y queda cacheado en ~/.cache/huggingface/.
sentence-transformers>=3.0.0
torch>=2.0.0

# Formatos adicionales
fpdf2
weasyprint>=60.0

# Entorno web (subcomando `python main.py web`)
flask>=3.0

# Navegación web y búsqueda (agentes Browser y Search)
# Tras instalar, Browser necesita el navegador Chromium de Playwright:
#     python -m playwright install chromium
# (en Linux puede requerir además: python -m playwright install-deps chromium)
playwright>=1.40
# El executor de búsqueda intenta primero 'ddgs' (paquete mantenido, antiguo
# 'duckduckgo-search') y cae al nombre antiguo si aquel no está.
ddgs>=9.0
